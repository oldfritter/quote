connect:
  host: 127.0.0.1
  port: 5672
  username: guest
  password: guest
  vhost: /quote # 注：为了不与其它项目冲突，使用vhost /quote，如果您不熟悉可以改为 /

exchange:
  default:
    key: quote.default
    type: direct
  fanout:
    default: quote.fanout
